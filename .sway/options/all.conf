# only to be stated in the configuration file for sway
# think of these as OPTIONs that are set by the configuration file

include {
    # {{{
    ~/.sway/options/general/misc.conf
    ~/.sway/options/components/bg.conf
    ~/.sway/options/components/nag.conf
    ~/.sway/options/components/bar.conf
    # }}}

    # {{{
    ~/.sway/options/general/layout.conf
    # }}}
}

# vim: foldmethod=marker
